#Define array of languages
lang_array=(ar ca cs de en es fa fi fr he it ja ko nl no pl pt ru sh sv tr uk vi zh)

#Check length
echo ${#lang_array[*]}
length_array=24

#Create some number of loops
for i in ${lang_array[*]}
do
python xml_interpreter(7).py -l $i
done